import HeroHeader from "./HeroHeader";
import SearchPanel from "./SearchPanel";

export default function Hero() {
  return (
    <section>
      <HeroHeader />
      <SearchPanel />
    </section>
  );
}
